# Copy to .env and set CLICKHOUSE_PASSWORD (not committed).
# cp .env.example .env
#
# Without CLICKHOUSE_USER + CLICKHOUSE_PASSWORD the image disables
# remote HTTP access for user `default`.

CLICKHOUSE_DB=sui_indexer
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=change-me
CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1
